dist: bionic
language: node_js
services:
# This is required to run a headful chrome/chromium
  - xvfb
node_js:
  - 12
cache:
  directories:
    - node_modules
    - test/node_modules
git:
  depth: 1
before_install:
  - "sysctl kernel.unprivileged_userns_clone=1"
install:
  - npm install
before_script:
  - "make"
  - "cd test"
#script:
#  - node_modules/.bin/jest --testTimeout=60000 example.test.js
