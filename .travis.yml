dist: bionic
language: node_js
services:
# This is required to run a headful chrome/chromium
  - xvfb
node_js:
  - 12
cache:
  directories:
    - node_modules
git:
  depth: 1
before_script:
  - "sysctl kernel.unprivileged_userns_clone=1"
  - "make"
